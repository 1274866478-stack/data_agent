# MCP ECharts Server Dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 安装 mcp-echarts 全局包
RUN npm install -g mcp-echarts

# 暴露端口
EXPOSE 3033

# 启动 MCP ECharts 服务器（SSE 模式，端口 3033）
CMD ["mcp-echarts", "-t", "sse", "-p", "3033"]




